export const sayHello = (name) => {
    console.log(`Open the pod bay doors, ${name}`);
}

export const respond = (name) => {
    console.log(`I'm afraid I can't let you do that, ${name}`);
}

export default sayHello;